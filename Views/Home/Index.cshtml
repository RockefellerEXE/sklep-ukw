@model IEnumerable<Film>
@{
    ViewData["Title"] = "Hej";
    ViewBag.Title = "Główna";
}

<div class="text-center">
    @foreach(var film in Model)
    {
        if(film.Poster != null)
        {
            <img class="posters" src="~/obrazki/@film.Poster" />
        }
    }
</div>
